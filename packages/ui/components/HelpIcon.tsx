import { Icon } from "native-base"
import { Entypo } from '@native-base/icons';

export const HelpIcon = (props: any) => <Icon {...props} size="sm" color="gray.300" as={Entypo} name="help-with-circle" />